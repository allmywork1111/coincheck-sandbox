namespace :coincheck_exchange_order do
  task :run => :environment do
    rate = 2873835.0
    amount = 0.005

    # CoincheckExchangeOrder.buy(
    #   rate: rate,
    #   amount: amount,
    # )
  end
end
